# GBEmu configuration
# Lines starting with # or ; are comments.
#
# Examples:
# system = auto   # auto | gb | gbc | gba
# fps = 59.7275   # override target FPS (0 disables pacing)
# scale = 4       # window scale factor
# video_driver = wayland  # wayland | x11
